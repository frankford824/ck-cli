# Harness 说明

ccli 的 Harness 是模型之外的外部支架，负责把智能体运行变成稳定流程：固定规则、限制工具、自动验证、受控返工、进度落盘和审计记录。

## 默认约束

- 模型可以参与规划、开发和审查，但用户界面只展示中文产品语义。
- 工具按阶段暴露，避免让模型在无关工具之间游移。
- 自动验证结果优先回流给开发代理，不直接推给普通用户判断。
- 每次任务都写入 `.ccli/progress.json`，长期经验写入项目记忆。
